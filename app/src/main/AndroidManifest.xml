<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sandipianadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/admin"
        android:label="@string/app_name"
        android:roundIcon="@drawable/admin"
        android:supportsRtl="true"
        android:theme="@style/Theme.SandipianAdmin">
        <activity android:name=".faculty.AddTeacher"></activity>
        <activity android:name=".faculty.UpdateFaculty" />
        <activity android:name=".UploadPdfActivity" />
        <activity android:name=".UploadImage" />
        <activity android:name=".UploadNotice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>